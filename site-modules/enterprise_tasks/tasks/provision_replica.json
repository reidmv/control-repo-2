{
  "description": "Provision a given host as a new HA replica",
  "files": ["ruby_task_helper/files/task_helper.rb"],
  "input_method": "stdin",
  "parameters": {
    "host": {
      "description": "The certname of the host",
      "type": "String[1]"
    },
    "replication_timeout": {
      "description": "The number of seconds to wait before timing out on service status checks",
      "type": "Integer"
    }
  }
}
