{
  "description": "Sign a certificate on the CA",
  "files": ["ruby_task_helper/files/task_helper.rb"],
  "input_method": "stdin",
  "parameters": {
    "allow_authorization_extensions" : {
      "description": "Allow the CSR to include authorization extensions.(default: true)",
      "type": "Optional[Boolean]"
    },
    "allow_dns_alt_names" : {
      "description": "Allow the CSR to include alt names.(default: true)",
      "type": "Optional[Boolean]"
    },
    "fingerprint": {
      "description": "The fingerprint of the CSR to sign",
      "type": "Optional[String[1]]"
    },
    "host": {
      "description": "The certname of the host to to sign",
      "type": "String[1]"
    }
  }
}
