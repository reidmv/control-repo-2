{
  "description": "Delete old certificate and submit a new CSR for and agent",
  "files": ["ruby_task_helper/files/task_helper.rb"],
  "input_method": "stdin",
  "parameters": {
    "cacert": {
      "description": "The certname of the host to clean",
      "type": "Optional[String[1]]"
    },
    "trust_ca": {
      "description": "Should the ca be verified(default: false)",
      "type": "Optional[Boolean]"
    },
    "fetch_only": {
      "description": "When true this will fetch info for the signed cert(default: false)",
      "type": "Optional[Boolean]"
    },
    "attrs": {
      "description": "csr attributes",
      "type": "Optional[Hash]"
    }
  }
}
