{
  "description": "Gets one or more values for existing keys in a HOCON conf file.",
  "files": ["ruby_task_helper/files/task_helper.rb"],
  "parameters": {
    "file": {
      "description": "The .conf file to fetch the value from.",
      "type": "String[1]"
    },
    "keys":{
      "description": "A string or array of strings of keys for which you want the value.",
      "type": "Variant[String[1],Array[String]]"
    }
  }
}